import { ArrowLeft, BookOpen } from 'lucide-react'
import { Link } from 'react-router-dom'
import { GEOMETRIA_STAGE } from '../../data/geometriaCourse'

export default function GeometrySyllabus() {
  return <main className="min-h-full bg-cream-100 px-4 py-6 sm:px-8 lg:px-12"><div className="mx-auto max-w-5xl"><Link to="/courses/geometria" className="inline-flex items-center gap-2 rounded-xl px-1 py-1 text-sm font-extrabold text-bark-500 hover:text-primary-500"><ArrowLeft size={17} />Volver a Geometría</Link><header className="mt-8"><p className="flex items-center gap-2 text-[11px] font-extrabold uppercase tracking-[0.16em] text-primary-500"><BookOpen size={15} />Temario PAP</p><h1 className="mt-2 font-display text-3xl font-extrabold text-bark-900 sm:text-5xl">Geometría</h1><p className="mt-3 max-w-2xl text-sm font-semibold leading-relaxed text-bark-500">Una vista general del recorrido. Los temas son informativos; las lecciones se realizan desde el árbol de aprendizaje.</p></header><div className="mt-8 space-y-5">{GEOMETRIA_STAGE.sections.map((section, index) => <section key={section.id} className="rounded-[1.75rem] border border-bark-100 bg-white p-5 shadow-card sm:p-7"><div className="flex items-start gap-4"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-primary-50 text-2xl">{section.icon}</span><div><p className="text-[10px] font-extrabold uppercase tracking-[0.14em] text-primary-500">Sección {index + 1}</p><h2 className="mt-1 font-display text-xl font-extrabold text-bark-800 sm:text-2xl">{section.title}</h2><p className="mt-1 text-sm font-semibold text-bark-400">{section.subtitle}</p></div></div><ul className="mt-5 grid gap-2 border-t border-bark-100 pt-5 sm:grid-cols-2">{section.nodes.map((node) => <li key={node.id} className="rounded-xl bg-cream-50 px-4 py-3 text-sm font-bold text-bark-600">{node.title}</li>)}</ul></section>)}</div></div></main>
}
